# Frontend
# REQUIRED: Get these from your Convex deployment (run `bunx convex dev`)
VITE_CONVEX_URL=
VITE_CONVEX_SITE_URL=

# Optional: Only needed for onlycrabs.ai (SOUL.md) mode
VITE_SOULHUB_SITE_URL=
VITE_SOULHUB_HOST=
VITE_SITE_MODE=

# Local development URL
SITE_URL=http://localhost:3000

# REQUIRED: Same as VITE_CONVEX_SITE_URL (used for auth cookies)
CONVEX_SITE_URL=

# Convex Auth (GitHub OAuth App)
# Optional for demo: Required only if you want to test login/auth features
# Create OAuth App at: https://github.com/settings/developers
# Callback URL: http://localhost:3000/api/auth/callback/github
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# Convex Auth JWT keys (generated via @convex-dev/auth CLI)
# Optional for demo: Run `bunx auth --deployment-name <deployment> --web-server-url http://localhost:3000`
JWT_PRIVATE_KEY=
JWKS=

# Embeddings
# Optional for demo: Required only for vector search functionality
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=
