name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    uses: ./.github/workflows/reusable-ci.yml
    with:
      bun-version: '1.3.6'
      run-lint: true
      run-tests: true
      run-coverage: true
      run-build: true
      timeout-minutes: 15
